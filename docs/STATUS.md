# Syto — текущее состояние

Этот файл — первое, что читает любая сессия. Держим его коротким и актуальным.

Обновлено: 2026-09-24

## Где мы

- Фаза 0 ✅. Текущая фаза: **1 — Голосовой спайк** (🔄, начата 2026-09-24).
- 1.1 автоответ: H1 подтверждена, 7/20. 1.2 говорить: **абонент слышит только на потоке MUSIC, и плохо; на VOICE_CALL — тишина**
  (гипотеза: AEC вычитает VOICE_CALL из микрофона). Сделаны правки: MUSIC по умолчанию, boost громкости, rate 0.9x, вариант ALARM.
  Ждут проверки живым звонком. 1.3–1.6 ⬜.
- Телефон отвалился от adb по Wi‑Fi; новая сборка ещё не установлена. Логи приходят файлом через Share.

## Следующий шаг

1. Пользователь: поставить новую сборку (adb, когда снова включит отладку по Wi‑Fi, или артефакт CI), позвонить с MUSIC
   (boost вкл.), потом с ALARM. Оценить разборчивость: чек-лист 1.2. Прислать `spike.log`.
2. Агент: по результату — либо фиксировать лучший вариант и идти в 1.3 (слышать), либо пробовать EARPIECE-маршрут и
   другие обходы из 1.6/P3.

## Что сделано за последнюю сессию

- 2026-09-24 (Mac): первый раунд 1.2 на живых звонках. Находка: VOICE_CALL абонент не слышит, MUSIC слышит плохо.
  Сделаны boost громкости, speech rate, ALARM-поток, MUSIC по умолчанию; DIAL-интенты уходят системной звонилке.
- 2026-09-24 (Mac): 1.1 подтверждена живым звонком (RINGING → answer → ACTIVE за 0,4 с, экран Syto поверх).
  Написана 1.2: `Talker` (TTS), приветствие после ответа через SPEAKER, отбой через 8 с, тумблеры потока/маршрута,
  кнопка проверки голоса. Google TTS на телефоне имеет `uk-UA`. Ждём звонка с проверкой слышимости.
- 2026-09-24 (Mac): начата фаза 1. Написаны 1.0 (журнал спайка, экран лога с Share) и 1.1 (роль звонилки,
  `InCallService` с автоответом незнакомым, in-call экран, разрешения, тумблер тестового режима).
  Поставлено на realme, приложение стартует. Автоответ живым звонком ещё не проверен.
- 2026-09-24 (Mac): **G0 пройден.** USB-кабель не дал данных, подключили adb по Wi‑Fi (pair по коду).
  Поставили локальную сборку, поверх неё — CI-сборку `af6343c` (run #4) без удаления. С экрана и `getprop`
  записаны Android 16 (API 36), realme RMX3999, realme UI V16.0.0. Фаза 0 закрыта, STATUS/PLAN/DECISIONS обновлены.
  Замечено: на телефоне Viber с определителем номера — в фазе 2 будет конкурировать за роль скрининга.
- 2026-09-24 (Mac): пользователь поставил Android Studio и открыл проект. Уведомление про Gradle JDK
  разобрано — не ошибка, студия сама взяла встроенный JBR 25. `./gradlew assembleDebug` локально зелёный,
  APK совпадает по размеру с CI. Телефон к Mac ещё не подключался. Подробности — в заметках фазы 0.
- 2026-09-24 (облако): CI run #1 зелёный с первой попытки, артефакт `syto-debug-72ee01a`. Пользователь
  сообщил: на телефоне Android 16 (API 36) — записано в D-006 и стенд фазы 1.
- 2026-09-24 (облако): начата фаза 0. Написан Gradle-проект и модуль `app`, экран с версией /
  хэшем / устройством, адаптивная векторная иконка из брендового SVG, CI workflow, инструкция
  по установке. Решения D-009 (коммитим debug keystore), D-010 (versionCode = 1 на время
  разработки), D-011 (AGP 9 built-in Kotlin). Выяснено: облако не может собирать — `dl.google.com`
  закрыт прокси, проверка только в CI.
- 2026-09-24: получены ответы на открытые вопросы, записаны как D-005..D-008.
  Стенд фазы 1 частично заполнен.
- 2026-09-24: составлен план планов (`docs/PLAN.md`), файлы фаз 0–6, шаблон фазы,
  журнал решений, `CLAUDE.md`. Решено идти voice-first (D-001).

## Блокеры

Нет.

## Открытые вопросы к пользователю

Ответ переносим в `DECISIONS.md` и удаляем вопрос отсюда.

1. Какой второй телефон найден для тестовых звонков (модель не важна, важно, чтобы был).
