# Syto — текущее состояние

Этот файл — первое, что читает любая сессия. Держим его коротким и актуальным.

Обновлено: 2026-09-24

## Где мы

- Текущая фаза: **0 — Каркас и конвейер** (🔄 в работе, начата 2026-09-24)
- Подфазы: 0.1 Gradle-проект — код написан · 0.2 CI — workflow написан · 0.3 установка — инструкция написана.
  Все три ждут первого зелёного прогона CI и проверки пользователем на телефоне.
- В репозитории есть Gradle-проект (AGP 9.4, Gradle 9.6.1, Kotlin 2.4.10, Compose BOM 2026.09,
  compileSdk 37, minSdk 29), модуль `app` с одним экраном, workflow сборки APK, debug keystore.

## Следующий шаг

1. Агент (эта сессия): дождаться прогона `Build debug APK` на ветке, починить, если красный,
   записать номер прогона в файл фазы.
2. Пользователь: скачать артефакт, поставить на realme по инструкции «Как ставить APK»
   в `docs/phases/phase-0-scaffold.md`, пройти чек-лист 0.3, вписать `Android` и `Device` с экрана
   в `DECISIONS.md` (гейт G0).
3. После G0 — фаза 1 в локальной сессии на Mac (D-008).

## Что сделано за последнюю сессию

- 2026-09-24 (облако): начата фаза 0. Написан Gradle-проект и модуль `app`, экран с версией /
  хэшем / устройством, адаптивная векторная иконка из брендового SVG, CI workflow, инструкция
  по установке. Решения D-009 (коммитим debug keystore), D-010 (versionCode = 1 на время
  разработки), D-011 (AGP 9 built-in Kotlin). Выяснено: облако не может собирать — `dl.google.com`
  закрыт прокси, проверка только в CI.
- 2026-09-24: получены ответы на открытые вопросы, записаны как D-005..D-008.
  Стенд фазы 1 частично заполнен.
- 2026-09-24: составлен план планов (`docs/PLAN.md`), файлы фаз 0–6, шаблон фазы,
  журнал решений, `CLAUDE.md`. Решено идти voice-first (D-001).

## Блокеры

Нет.

## Открытые вопросы к пользователю

Ответ переносим в `DECISIONS.md` и удаляем вопрос отсюда.

1. Версия Android и версия realme UI на RMX3999. Версию Android и API level теперь показывает
   сам экран Syto после установки; версию realme UI смотреть в Настройки → О телефоне. Вписать в стенд фазы 1.
2. Какой второй телефон найден для тестовых звонков (модель не важна, важно, чтобы был).
